import AddItemForm from "@/components/forms/AddItemForm";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return <AddItemForm />;
};

export default page;
